rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn()
        && request.auth.token.admin == true;
    }

    function isBootstrapAdmin() {
      return isSignedIn()
        && request.auth.token.email in [
          "john.cyl@gmail.com"
        ];
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    match /shareholders/{userId} {
      allow read: if isOwner(userId) || isAdmin() || isBootstrapAdmin();
      allow write: if isAdmin() || isBootstrapAdmin();
    }

    match /shareholderDrafts/{shareholderCode} {
      allow read, write: if isAdmin() || isBootstrapAdmin();
    }

    match /statements/{userId}/months/{monthId} {
      allow read: if isOwner(userId) || isAdmin() || isBootstrapAdmin();
      allow write: if isAdmin() || isBootstrapAdmin();
    }

    match /portalMonthlyReports/{reportId} {
      allow read: if isSignedIn();
      allow write: if isAdmin() || isBootstrapAdmin();
    }

    match /portalCategories/{categoryId} {
      allow read: if isSignedIn();
      allow write: if isAdmin() || isBootstrapAdmin();
    }

    match /portalTopics/{topicId} {
      allow read: if isSignedIn();
      allow write: if isAdmin() || isBootstrapAdmin();
    }

    match /portalAnnouncements/{announcementId} {
      allow read: if isSignedIn();
      allow write: if isAdmin() || isBootstrapAdmin();
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
